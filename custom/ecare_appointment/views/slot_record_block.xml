<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>

        <!-- Explicit Form View -->

        <record id="block_slot_form_view" model="ir.ui.view">
            <field name="name">ec.booked.slot.record.form.view</field>
            <field name="model">ec.booked.slot.record</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>

                            <field name="date" readonly="1"/>
                            <field name="start" readonly="1"/>
                            <field name="stop" readonly="1"/>
                            <!-- Making it pointer-events None made it non-clickable even in when it should so be careful -->
                            <field name="category" readonly="1"
                                   style="pointer-events:none;"/>

                            <field name="sub_category" readonly="1"
                                   style="pointer-events:none;"/>
                            <!-- Pointer-EVents NOne -->

                            <field name="comment"
                                   requried="True"
                            />
                            <field name="state" readonly="1" invisible="1"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Explicit Action -->

        <record id="block_slot_action" model="ir.actions.act_window">
            <field name="name">Block Appointment</field>
            <field name="res_model">ec.booked.slot.record</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <field name="context">{'default_state': 'Blocked'}</field>
            <field name="view_id" ref="block_slot_form_view"/>
        </record>
    </data>
</odoo>